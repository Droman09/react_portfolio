import { Parallax } from 'react-parallax';

const ParallaxImage = () => (
    <Parallax  bgImage= "" bgImageAlt="profile pic" strength={200} >
        <div style={{height: 350}}>
         
        </div>

    </Parallax>
);

export default ParallaxImage